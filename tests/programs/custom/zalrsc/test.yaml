name: zalrsc
description: Sanity checks for the ZALRSC extension. TODO, krdosvik, use CFG=zalrsc_ext as long as issue ... is open. When closed use clic_default config.
uvm_test: uvmt_$(CV_CORE_LC)_firmware_test_c
